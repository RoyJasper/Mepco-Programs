#include<stdio.h>
#include<string.h>
void main()
{
 file *fp1,fp;
 fp=fopen("file.txt","r");
 fscanf(fp1,"%c",s);
 while(j=='m')
{
 fscanf(fp1,"%s",string);
 printf("ENTER THE RELOCATION NUMBER");
 scanf("%s",rl);
 func(string,rl);
}
}
void func(char[] arr,char []addr)
{
 file *fp;
fp=fopen("binary.bin",",");
while(fp=fgetc(fp)!=EOF)
{
 fseek(fp,sizeof(nodes)*i,seek_set);
fread(&nn,sizeof(node)*i,fp);
if(strcmp(array,nn->addr)==0)
{
 for(j=0;j<3;j++)
{
 string1=convert(nn->value1);
 string2=convert(nn->value2);
a[i]=string1;
a[j+1]=strin2;
fseek(fp1,sizeof(node)*j;seek->set);
fread(&nn,sizeof(node)*i,fp2);
}
 

